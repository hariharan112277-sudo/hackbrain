# =============================================================================
# IOB Data Engine - Stage 1 Dependencies
# =============================================================================

# MQTT broker client (publisher + subscriber)
paho-mqtt>=2.0.0

# Pydantic for strict schema validation (v1 or v2 compatible)
pydantic>=1.10,<3.0

# Pydantic Settings v2 for enterprise configuration management
pydantic-settings>=2.0.0

# PostgreSQL driver (sync)
psycopg2-binary>=2.9.0

# SQLAlchemy ORM (Track A — Database Layer)
SQLAlchemy>=2.0.0,<3.0

# YAML configuration
PyYAML>=6.0

# FastAPI & ASGI server (Phase 3 Core Foundation)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
starlette>=0.27.0

# --- Security Dependencies (Phase 3) ---
# JWT encoding/decoding
python-jose[cryptography]>=3.3.0
# Password hashing (Argon2 + Bcrypt)
passlib[argon2]>=1.7.4
argon2-cffi>=21.3.0
# passlib 1.7.x is incompatible with bcrypt 4.1+ backend detection.
bcrypt>=4.0.0,<4.1.0

# Tests
pytest>=7.0

# --- Phase 4 Enterprise Backend API Dependencies ---
# OAuth2 form parsing & multipart uploads
python-multipart>=0.0.9
# Email validation for Pydantic EmailStr
email-validator>=2.0.0
# HTTP client for test suite
httpx>=0.27.0
